version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.12"
  commands:
    - pip install uv
    - uv pip install --system --group build_doc -e .
    - sphinx-build -b html docs $READTHEDOCS_OUTPUT/html
